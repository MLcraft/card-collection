spring:
  application:
    name: cardcollection
  datasource:
    # url: jdbc:h2:mem:test
    url: jdbc:h2:mem:mydb;DB_CLOSE_DELAY=-1;
    driver-class-name: org.h2.Driver
    username: sa
    password: password